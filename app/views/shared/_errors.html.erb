<% if instance.errors.any? %>
  <div style="color: red">
    <h3><%= pluralize(instance.errors.count, "error") %> prohibited this <%= instance.class.name.downcase %> from being saved:</h3>
      <% instance.errors.full_messages.each do |message| %>
        <p><%= message %></p>
      <% end %>
  </div>
<% end %>
